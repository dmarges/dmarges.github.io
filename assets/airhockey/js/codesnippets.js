    
    //For collision detection for objects.
    /*this.ctx.fillStyle = '#00FF00';
    this.ctx.beginPath();
    this.ctx.arc(this.playerPaddleObj.x, this.playerPaddleObj.y, this.playerPaddleObj.radius, 0, (Math.PI * 2), false);
    this.ctx.closePath();
    this.ctx.fill();
    

    this.ctx.fillStyle = '#0000FF';
    this.ctx.beginPath();
    this.ctx.arc(this.puckObj.x, this.puckObj.y, this.puckObj.radius, 0, (Math.PI * 2), false);
    this.ctx.closePath();
    this.ctx.fill();*/
    
    
    //For goal area objects
    /*
    this.ctx.beginPath();
    this.ctx.rect(970, 180, 50, 415);
    this.ctx.fillStyle = 'red';
    this.ctx.fill();
    this.ctx.lineWidth = 1;
    this.ctx.strokeStyle = 'black';
    this.ctx.stroke();
    
    this.ctx.beginPath();
    this.ctx.rect(0, 180, 50, 415);
    this.ctx.fillStyle = 'blue';
    this.ctx.fill();
    this.ctx.lineWidth = 1;
    this.ctx.strokeStyle = 'black';
    this.ctx.stroke();
    
    this.ctx.beginPath();
    this.ctx.rect(490, 180, 20, 415);
    this.ctx.fillStyle = 'blue';
    this.ctx.fill();
    this.ctx.lineWidth = 1;
    this.ctx.strokeStyle = 'black';
    this.ctx.stroke();*/

        //Rectangle collision
    /*if (object1.x < object2.x + object2.width  && object1.x + object1.width  > object2.x &&
        object1.y < object2.y + object2.height && object1.y + object1.height > object2.y) {

        object2.vX = object1.vX;
        object2.vY = object1.vY;
    }*/
